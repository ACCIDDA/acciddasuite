# Print an `accidda_data` object

Print an `accidda_data` object

## Usage

``` r
# S3 method for class 'accidda_data'
print(x, ...)
```

## Arguments

- x:

  An `accidda_data` object.

- ...:

  Ignored.
