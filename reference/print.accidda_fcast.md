# Print an `accidda_fcast` object

Print an `accidda_fcast` object

## Usage

``` r
# S3 method for class 'accidda_fcast'
print(x, ...)
```

## Arguments

- x:

  An `accidda_fcast` object.

- ...:

  Ignored.
