# Print an accidda_cast object

Displays a concise summary of an `accidda_cast` object, including model
scores, forecast horizon, and hub format contents.

## Usage

``` r
# S3 method for class 'accidda_cast'
print(x, ...)
```

## Arguments

- x:

  An object of class `accidda_cast`.

- ...:

  Additional arguments (currently ignored).
