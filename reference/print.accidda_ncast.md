# Print an `accidda_ncast` object

Print an `accidda_ncast` object

## Usage

``` r
# S3 method for class 'accidda_ncast'
print(x, ...)
```

## Arguments

- x:

  An `accidda_ncast` object.

- ...:

  Ignored.
