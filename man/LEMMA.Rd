% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lemma.R
\name{LEMMA}
\alias{LEMMA}
\title{LEMMA-forecast model for fable}
\usage{
LEMMA(
  formula,
  approach = "Flatline",
  ensemble_method = "Random Forest",
  quantiles = c(0.025, 0.1, 0.25, 0.5, 0.75, 0.9, 0.975),
  timesteps_per_bin = NULL,
  reference_date = NULL,
  season_start_date = NULL,
  hyperparams = list(),
  n_sim = 1000L
)
}
\arguments{
\item{formula}{Response variable, e.g. \code{observation}. Exogenous
regressors are not supported.}

\item{approach}{Predictor approach name as understood by LEMMA
(\code{"Flatline"}, \code{"ARIMA"}, \code{"SIKJalpha"}, \code{"VAR_UN"}).}

\item{ensemble_method}{Ensemble aggregator (\code{"Random Forest"} by
default; see LEMMA's \code{config_model} for alternatives).}

\item{quantiles}{Output quantile levels passed to LEMMA. Must lie in
\verb{[0, 1]}. Finer grids give better sample reconstruction.}

\item{timesteps_per_bin}{Number of base timesteps per forecast bin.
If \code{NULL} (the default), it is auto-detected from the tsibble index
spacing (e.g. 7 for weekly data, 1 for daily data).}

\item{reference_date}{Reference date used internally by LEMMA for date
arithmetic. \code{NULL} keeps LEMMA's default. Accepts \code{Date} or strings
coercible to \code{Date}.}

\item{season_start_date}{Season start date, used by \code{"SIKJalpha"} to
initialise its mechanistic component. \code{NULL} keeps LEMMA's default.
Accepts \code{Date} or strings coercible to \code{Date}.}

\item{hyperparams}{Named list of approach-specific hyperparameters,
forwarded verbatim to the generated \code{user_config.py}. Entries override
LEMMA's defaults. See LEMMA's \code{user_config.py} for the full list
(e.g. \code{arima_autoregressive_orders}, \code{flatline_lag_timesteps},
\code{halpha_list}, \code{un_list}, \code{hk}, \code{hjp}).}

\item{n_sim}{Number of samples drawn from LEMMA's quantile grid per
horizon to build the \code{dist_sample} distribution returned from
\code{forecast()}.}
}
\value{
A model definition for use inside \link[fabletools:model]{model()}.
}
\description{
Fits \href{https://github.com/ACCIDDA/LEMMA-forecast}{LEMMA-forecast} through
the fable interface. Training stores the series and settings; \code{forecast()}
writes the series to a Hubverse-shaped CSV, runs LEMMA's \code{main.py} in a
dedicated Python virtualenv (via a subprocess), reads LEMMA's quantile
output back, and turns it into a sample-based \code{distributional} object
compatible with \code{fabletools::forecast()} and the mixture ensembles built
by \code{\link[=get_fcast]{get_fcast()}}.
}
\details{
Requires \code{\link[=install_lemma]{install_lemma()}} to have been run once to set up the
LEMMA-forecast clone and the Python dependencies.
}
