% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epiestim.R
\name{EPIESTIM}
\alias{EPIESTIM}
\title{EpiEstim model for fable}
\usage{
EPIESTIM(
  formula,
  mean_si,
  std_si,
  rt_window = 14L,
  n_sim = 100L,
  R_fix_within = TRUE
)
}
\arguments{
\item{formula}{Response variable to model, for example
\code{observation}. External predictors are not supported.}

\item{mean_si}{Mean serial interval in days.}

\item{std_si}{Standard deviation of the serial interval in days.}

\item{rt_window}{Length of the sliding window (in days) used to estimate Rt.
Smaller values respond more quickly to recent changes, while larger values
provide smoother estimates. Defaults to \code{14}.}

\item{n_sim}{Number of simulated forecast trajectories. Defaults to
\code{100}.}

\item{R_fix_within}{Logical indicating whether Rt is held constant within
each simulated trajectory. Recommended for short forecast horizons.}
}
\value{
A \code{fable} model specification for use with
\code{\link[fabletools]{model}}.
}
\description{
Create a \code{fable} model that estimates the effective reproduction number
(Rt) using \code{EpiEstim::estimate_R()} and generates forecasts using
\code{projections::project()}.
}
\details{
Rt is estimated from the most recent observations, then future trajectories
are generated by simulating from the estimated Rt distribution. Forecasts
are returned as sample distributions compatible with \code{fable}.
}
\examples{
\dontrun{
example_data |>
  get_fcast(models = EPIESTIM(
    observation,
    mean_si = 4.7,
    std_si = 2.9,
    rt_window = 7
  ))
}

}
\author{
Cyril Geismar
}
